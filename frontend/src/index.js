import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App";
import { BrowserRouter } from "react-router-dom";
import { AuthProvider } from "./context/AuthContext";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <BrowserRouter>
    <AuthProvider>
      <App />
    </AuthProvider>
  </BrowserRouter>
);

// This code initializes a React application by rendering the App component inside a BrowserRouter and AuthProvider.
// The BrowserRouter enables routing capabilities, allowing navigation between different components based on the URL.       
